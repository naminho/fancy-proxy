{
  "name": "fancy-proxy-demo-cra",
  "scripts": {
    "start": "npm run copy:src && react-scripts start",
    "build": "npm run copy:src && react-scripts build",
    "copy:src": "cp -a ../src .",
    "local": "npm install --no-save $(npm pack ../.. | tail -1)"
  },
  "dependencies": {
    "@babel/polyfill": "^7.10.4",
    "fancy-proxy": "^0.1.1",
    "react": "^16.13.1",
    "react-dom": "^16.13.1",
    "react-scripts": "3.4.1"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ]
}
